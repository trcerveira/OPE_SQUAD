# Story 1.2 — Copiar Shared Libs (Supabase, Validators, Config)

> Epic: PUBLYQ MVP
> Priority: P0 | Points: 2
> Dependencies: Story 1.1
> Assigned to: @dev

---

## User Story

**As a** developer,
**I want to** copy and adapt the shared libraries from OPB Crew,
**So that** all API routes have access to Supabase client, rate limiting, audit logging, validation, and admin config.

---

## Acceptance Criteria

- [ ] `lib/supabase/server.ts` — createServerClient with service_role (copy as-is from OPB Crew)
- [ ] `lib/supabase/rate-limit.ts` — DB-based rate limiter (copy as-is)
- [ ] `lib/supabase/audit.ts` — Fire-and-forget audit logger (copy as-is)
- [ ] `lib/supabase/types.ts` — TypeScript types for PUBLYQ tables (adapt for new schema)
- [ ] `lib/validators/index.ts` — New Zod schemas for Brand DNA, Voice DNA, Carousel (Zod v4)
- [ ] `lib/config/admins.ts` — SUPER_ADMINS + BETA_USERS (copy as-is, same users)
- [ ] `lib/supabase/user-profiles.ts` — getUserProgress(), syncUserProfile(), saveBrandProfile(), saveVoiceProfile()
- [ ] `lib/utils/helpers.ts` — Utility functions if needed
- [ ] All imports resolve correctly
- [ ] TypeScript compiles without errors

---

## Technical Notes

### Source Files (OPB Crew)
| OPB Crew | PUBLYQ | Reuse % |
|----------|--------|---------|
| `lib/supabase/server.ts` | `lib/supabase/server.ts` | 100% |
| `lib/supabase/rate-limit.ts` | `lib/supabase/rate-limit.ts` | 100% |
| `lib/supabase/audit.ts` | `lib/supabase/audit.ts` | 100% |
| `lib/supabase/types.ts` | `lib/supabase/types.ts` | 40% (new tables) |
| `lib/validators/index.ts` | `lib/validators/index.ts` | 60% (new schemas) |
| `lib/config/admins.ts` | `lib/config/admins.ts` | 100% |
| `lib/supabase/user-profiles.ts` (OPB Crew) | `lib/supabase/user-profiles.ts` | 60% (new functions) |

### Zod v4 Rules (CRITICAL)
- NO `required_error` in `z.string()` — use `.min(1, "msg")` instead
- Use `.issues[0]` not `.errors[0]` on ZodError

### New Zod Schemas Needed
```typescript
// Brand DNA
BrandDNASchema: brand_name, mission, values[], target_audience, positioning, personality{}, differentiator

// Voice DNA
VoiceDNASchema: answers (8 questions — same structure as OPB Crew)

// Carousel
CarouselSchema: topic (string min 3), keywords[] (optional)
```

---

## Files to Create

| File | Action |
|------|--------|
| `lib/supabase/server.ts` | Copy from OPB Crew |
| `lib/supabase/rate-limit.ts` | Copy from OPB Crew |
| `lib/supabase/audit.ts` | Copy from OPB Crew |
| `lib/supabase/types.ts` | Create new (PUBLYQ schema) |
| `lib/validators/index.ts` | Create new (PUBLYQ schemas) |
| `lib/config/admins.ts` | Copy from OPB Crew |
| `lib/supabase/user-profiles.ts` | Create new (adapt from OPB Crew) |

---

## Definition of Done

- [ ] All lib files exist and import correctly
- [ ] TypeScript compiles without errors
- [ ] Zod schemas match ARCH-PUBLYQ database schema

---

*— River, removendo obstaculos*
