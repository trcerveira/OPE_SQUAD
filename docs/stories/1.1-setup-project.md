# Story 1.1 — Setup Projecto Next.js + Configuracao Base

> Epic: PUBLYQ MVP
> Priority: P0 | Points: 3
> Dependencies: None
> Assigned to: @dev

---

## User Story

**As a** developer,
**I want to** create a fresh Next.js project with all required dependencies configured,
**So that** all subsequent features have a solid foundation to build on.

---

## Acceptance Criteria

- [x] New Next.js 16 project created (TypeScript, App Router, Tailwind CSS)
- [x] Dependencies installed: `@clerk/nextjs`, `@supabase/supabase-js`, `@anthropic-ai/sdk`, `zod`, `html-to-image`, `jszip`, `file-saver`, `svix`
- [x] `tailwind.config.ts` configured with PUBLYQ brand colors:
  - bg: `#0A0E1A`, surface: `#111827`, accent: `#BFD64B`, text: `#F0ECE4`, muted: `#8892A4`
- [x] `globals.css` with brand CSS variables and base styles
- [x] `layout.tsx` with Space Grotesk font (next/font), PT lang, dark theme
- [x] `.env.local` template created with all required env vars (no real secrets)
- [x] `.gitignore` configured (node_modules, .env.local, .next, etc.)
- [x] `next.config.ts` configured (image domains for Unsplash)
- [x] Project builds without errors (`npm run build`)
- [x] Dev server starts without errors (`npm run dev`)

---

## Technical Notes

- **Fresh project** — do NOT copy OPB Crew wholesale, create clean
- Font: Space Grotesk via `next/font/google`
- Tailwind: extend theme with brand colors
- Image domains: `images.unsplash.com`
- Target: **separate repo** `trcerveira/publyq` (confirmed by Telmo 2026-03-12)

---

## Files to Create/Modify

| File | Action |
|------|--------|
| `package.json` | Create (npm init + deps) |
| `tailwind.config.ts` | Create (brand colors) |
| `app/layout.tsx` | Create (root layout) |
| `app/globals.css` | Create (brand styles) |
| `next.config.ts` | Create (image domains) |
| `tsconfig.json` | Create (auto by Next.js) |
| `.env.local.example` | Create (template) |
| `.gitignore` | Create |

---

## Definition of Done

- [x] `npm run build` passes
- [x] `npm run dev` starts without errors
- [x] Brand colors visible in browser
- [x] Space Grotesk font loads correctly

---

*— River, removendo obstaculos*
